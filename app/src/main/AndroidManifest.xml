<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.grupo14.tpfinalg14">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.TPFinalG14"
        android:enableOnBackInvokedCallback="true"
        tools:targetApi="33">

        <!-- SplashActivity bloqueada en orientación vertical -->
        <activity android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- MainActivity contenedora de fragments -->
        <activity android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait" />

        <!-- LoginActivity en vertical -->
        <activity android:name=".ui.login.LoginActivity"
            android:screenOrientation="portrait" />

        <!-- RegisterActivity en vertical -->
        <activity android:name=".ui.registro.RegisterActivity"
            android:screenOrientation="portrait" />

    </application>
</manifest>