<?xml version="1.0" encoding="utf-8"?>

<!--
Layout para ítem del RecyclerView de vencimientos próximos.
Representa el resumen de una materia con su nombre y la fecha del próximo trabajo/tarea a entregar.
Utiliza CardView para estilizar la presentación con bordes redondeados y sombra, y ConstraintLayout para centrar los textos.
-->

<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="18dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="6dp">      <!-- Le da profundidad visual -->

    <!-- Contenedor flexible para ubicar el texto centrado -->
<androidx.constraintlayout.widget.ConstraintLayout
android:layout_width="match_parent"
android:layout_height="wrap_content"
android:padding="12dp">

<!-- Nombre de la materia -->
<TextView
    android:id="@+id/textNombreMateria"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Nombre de la materia"
    android:textSize="18sp"
    android:textStyle="bold"
    android:textColor="@android:color/black"
    android:gravity="center"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent" />

<!-- Información del próximo vencimiento -->
<TextView
    android:id="@+id/textProximoVencimiento"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Próximo vencimiento"
    android:textSize="14sp"
    android:textColor="@android:color/darker_gray"
    android:gravity="center"
    app:layout_constraintTop_toBottomOf="@id/textNombreMateria"
    app:layout_constraintStart_toStartOf="parent"
    app:layout_constraintEnd_toEndOf="parent"
    app:layout_constraintBottom_toBottomOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>