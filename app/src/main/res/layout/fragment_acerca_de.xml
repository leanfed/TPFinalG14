<?xml version="1.0" encoding="utf-8"?>

<!--
Layout para el fragmento "Acerca de" o vista de privacidad.
Utiliza ConstraintLayout como base visual, con un botón de menú arriba a la derecha, una CardView centrada
que actúa como contenedor del texto de política de privacidad y una BottomBar reutilizable al pie.
-->

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="19dp"
    android:background="@drawable/acerca_de_bg"> <!-- Fondo personalizado con  imagen -->

    <!-- ☰ Menú contextual superior derecho -->
    <include
        layout="@layout/menu_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- CardView principal centrada verticalmente con bias -->
    <androidx.cardview.widget.CardView
        android:id="@+id/cardPoliticaPrivacidad"
        android:layout_width="371dp"
        android:layout_height="560dp"
        android:layout_marginTop="40dp"
        android:padding="16dp"
        app:cardCornerRadius="12dp"
        app:cardElevation="6dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.7"> <!-- Desplazada hacia abajo para evitar que quede totalmente centrada -->

        <!-- Área con scroll para mostrar el texto largo de la política -->
        <androidx.core.widget.NestedScrollView
            android:layout_width="match_parent"
            android:layout_height="544dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="8dp">

                <TextView
                    android:id="@+id/textPoliticaPrivacidad"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text=""
                android:textColor="@android:color/black"
                android:textSize="16sp" />
            </LinearLayout>
        </androidx.core.widget.NestedScrollView>
    </androidx.cardview.widget.CardView>

    <!-- Footer: barra de navegación inferior reutilizable -->
    <include
        android:id="@+id/bottom_bar_include"
        layout="@layout/bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>